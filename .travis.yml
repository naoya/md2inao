language: perl
perl:
  - "5.18"
  - "5.16"
before_install:
  - cpanm -n git://github.com/kan/coveralls-perl.git
script: yes | (cpanm --installdeps . && cover -test)
after_success:
  - cover -report coveralls
notifications:
  email:
    - i.naoya+md2inao@gmail.com
  irc:
    - "irc.freenode.net#md2inao"
